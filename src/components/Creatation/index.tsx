import React, { Dispatch, SetStateAction, useEffect, useRef } from 'react';
import { World } from '../../World';
import styles from './index.module.scss';

type Props = {
  title:string
}

export const Creatation = ({title}:Props) => {

  return <div>Creatation</div>
}